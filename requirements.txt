# Signal-Search 运行依赖
# 安装：pip install -r requirements.txt
# 或（含开发依赖）：pip install -e ".[dev]"

# 核心：正文抽取 / 反爬抓取 / HTML 解析
trafilatura>=1.12.0
curl_cffi>=0.7.0
requests>=2.31.0
lxml>=5.0.0
markdownify>=0.13.0
beautifulsoup4>=4.12.0

# 可选：语义去重 / 语义级事实核验（M51）。缺失自动降级为关键词基线，不报错。
# sentence-transformers>=2.5.0
